Place a test image named `photo.jpg` in this folder so the photo upload test can run.
You can add any small JPEG image and name it `photo.jpg`.

Example: copy a local image to `client/cypress/fixtures/photo.jpg` before running tests.